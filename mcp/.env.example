# Railway จะกำหนด PORT ให้เอง ไม่ต้องตั้งเอง ยกเว้นรันในเครื่อง
PORT=8000
# ที่เก็บ log ของ log_exception (ถ้าอยากให้ข้อมูลอยู่ถาวร ให้ชี้ไป volume ของ Railway)
LOG_PATH=/tmp/retail_sop_log.jsonl
# โดเมนจริงหลัง deploy คั่นด้วยจุลภาค เว้นว่างตอนทดสอบในเครื่อง
ALLOWED_HOSTS=
